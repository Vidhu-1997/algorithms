pipeline{
    agent any   // don't have any ec2 instance so run it in any
    stages{
        stage('first-test-stage'){
            steps{
                bat 'whoami'
                bat 'where git.exe'
            }
        }
        stage('build'){
            steps{
                bat 'echo updated'
            }
        }
    }
}
