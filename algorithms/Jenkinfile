pipeline { 

    agent any 
    stages { 
        stage('Build') { 
            steps {
               def path1 = tool name: 'cmake25', type: 'hudson.plugins.cmake.CmakeTool'
               echo '${path1}'
               bat 'gcc --version'
               bat 'cmake --version'
            }
        }   
    }
}
  
