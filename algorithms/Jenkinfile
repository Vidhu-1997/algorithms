pipeline { 

    agent any 
    stages { 
        stage('Build') { 
            steps {
               def path = tool name: 'cmake25', type: 'hudson.plugins.cmake.CmakeTool'
               echo '${path}'
               bat 'gcc --version'
               bat 'cmake --version'
            }
        }   
    }
}
  
