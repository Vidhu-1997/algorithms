pipeline { 
     tools {
        cmake 'hudson.plugins.cmake.CmakeTool'
    }
    agent any 
    stages { 
        stage('Build') { 
            steps {
                   bat 'echo gcc --version' 
            }
        }   
    }
}
  
