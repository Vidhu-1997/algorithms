pipeline { 

    agent any 
    stages { 
        stage('Build') { 
            steps {
                tool name: 'cmake25', type: 'hudson.plugins.cmake.CmakeTool'
                bat 'cmake --version'
                bat 'echo gcc --version'
            }
        }   
    }
}
  
