pipeline { 
     tools {
        cmake 'cmake25'
    }
    agent any 
    stages { 
        stage('Build') { 
            steps {
                   bat 'echo gcc --version' 
            }
        }   
    }
}
  
