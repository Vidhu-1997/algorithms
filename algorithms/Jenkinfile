pipeline { 

    agent any 
    stages { 
        stage('Build') { 
            steps {
                tool name: 'cmake25', type: 'hudson.plugins.cmake.CmakeTool'
               bat 'gcc --version'
               bat 'cmake --version'
            }
        }   
    }
}
  
