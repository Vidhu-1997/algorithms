pipeline { 

    agent any 
    stages { 
        stage('Build') { 
            steps {
               script{
               def path1 = tool name: 'cmake25', type: 'hudson.plugins.cmake.CmakeTool'
              bat "cd /d ${path1} && cmake --version"
               bat 'gcc --version'
        bat " pushed algorithsm && cd /d ${path1} && cmake --build %CD% && pop"


               }
            }
        }   
    }
}
  
