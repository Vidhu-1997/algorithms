pipeline { 
     tools {
        cmake "cmake"
    }
    agent any 
    stages { 
        stage('Build') { 
            steps {
                   bat 'echo gcc --version' 
            }
        }   
    }
}
  
